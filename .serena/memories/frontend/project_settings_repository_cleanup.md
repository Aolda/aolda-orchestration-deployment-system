2026-04-17: User rejected the Project Settings 'connected repositories' table because it showed fake seed data like https://github.com/aolda-demo/project-a-api as if it were a live connection. Decision: remove the entire repository section from frontend/src/App.tsx ProjectSettingsPanel and stop fetching /api/v1/projects/{projectId}/repositories in the project workspace refresh flow. Keep the backend endpoint for contract compatibility, but default fixtures/seed catalogs should not include fake repositories anymore. Actual repository existence/connection validation happens in the application creation GitHub flow by reading aolda_deploy.json from the remote repository; do not reintroduce a separate project-level 'connected repository' UI unless it is backed by real validated data.