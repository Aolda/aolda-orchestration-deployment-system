Updated frontend App rules UX so LoadBalancer is treated as a high-impact request rather than an immediate external exposure toggle. Changes in frontend/src/App.tsx: label changed from 'LoadBalancer 노출'/'LB 공개' semantics to 'LoadBalancer 노출 요청' and catalog badge 'LB 요청'; enabling the switch now opens a confirmation modal with cost/external exposure/floating IP follow-up acknowledgement before draft state is set; rules tab shows a four-step workflow card (AODS save -> Flux sync -> cluster LB prep -> network floating IP/routing) explaining that AODS manages request persistence and GitOps, while actual floating IP / external routing completion happens outside AODS. Also logged in docs/user-feedback-log.md as FB-042.