Local runtime mismatch issue: frontend sent new PATCH fields `meshEnabled` / `loadBalancerEnabled`, but an older backend process on :18080 returned `INVALID_REQUEST` with details `json: unknown field "meshEnabled"`. Fixes: 1) frontend/src/api/client.ts now carries ErrorResponse.details on ApiError; 2) frontend/src/App.tsx translates unknown-field INVALID_REQUEST responses during network settings save into a clear backend-restart message; 3) local backend process was restarted with current code, after which PATCH /api/v1/applications/project-a__moltbot-front-poc-web with network fields returned 200 OK.