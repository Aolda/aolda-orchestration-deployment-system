2026-04-17: 사용자 결정으로 애플리케이션 CPU/메모리 request/limit은 저장소 descriptor(aolda_deploy.json)에서 관리하지 않고 AODS 운영 admin이 관리한다. 새 앱 생성 시 backend/internal/application/resources.go 의 기본값(requests cpu 250m, memory 256Mi / limits cpu 500m, memory 512Mi)을 자동 부여한다. 이후 수정은 PATCH /api/v1/applications/{applicationId} 의 resources 필드로만 가능하며 project admin 권한이 추가로 필요하다. Frontend는 운영 센터 > 운영 규칙 탭에 리소스 할당 카드가 있고, deployer는 롤백 정책은 저장할 수 있어도 resources는 저장할 수 없다. 관련 파일: backend/internal/application/resources.go, service.go, store_local.go, store_support.go, frontend/src/App.tsx, frontend/src/types/api.ts.