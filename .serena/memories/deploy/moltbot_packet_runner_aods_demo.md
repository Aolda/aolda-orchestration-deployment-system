2026-04-17: Deployed moltbot-front-poc to local AODS-connected shared namespace via the live manifest repo path that still uses projectId `project-a` (namespace `shared`). Because GitHub descriptor images were not published and GHCR push token lacked write:packages, used quick-create with ephemeral ttl.sh images instead. Created AODS apps: `project-a__moltbot-api-demo` and `project-a__moltbot-web-demo`. Current synced image refs: API `ttl.sh/moltbot-front-poc-api-aods-20260417-0501:24h`, Web `ttl.sh/moltbot-front-poc-web-aods-20260417-0504:24h`. Needed code fixes in external repo `/Users/ichanju/Desktop/aolda/moltbot-front-poc`: API now returns 200 on `/` for default AODS probes; web Dockerfile sets `HOSTNAME=0.0.0.0` so port-forward works. Local access while sessions are alive: AODS UI `http://localhost:5173`, deployed web app `http://localhost:33000`, deployed API `http://localhost:38080`. Long-running sessions currently expected: AODS backend session 99085, AODS frontend session 43986, api port-forward session 80469, web port-forward session 45986. Note: ttl.sh images expire in 24h, so this is PoC-only unless moved to persistent registry.