AODS (aolda-orchestration-deployment-system) is an internal app deployment platform. Repo structure: docs/ contains contracts and domain rules; frontend/ is a React + Vite TypeScript portal; backend/ is a Go service scaffold; AGENTS.md defines that docs and OpenAPI/domain rules must be treated as contracts. Current repo appears early-stage: root contains Makefile, README.md, AGENTS.md, frontend/, backend/, docs/. backend has go.mod only plus cmd/internal/pkg directories; frontend has standard Vite React app structure. Scripts/config directories referenced in AGENTS.md are not currently present in the workspace.