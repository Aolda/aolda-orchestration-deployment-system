apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aods-iiv
spec:
  provider:
    vault:
      server: http://10.16.254.243:8200
      path: secret
      version: v2
      auth:
        tokenSecretRef:
          name: aods-backend-secrets
          namespace: aods-system
          key: AODS_IIV_TOKEN
